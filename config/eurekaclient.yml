server:
  port: 8764

spring:
  application:
    name: eurekaclient
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: guest
    password: guest
    virtualHost: /
    publisherConfirms: true
    
song:
  name: song_ra

management:
  endpoints:
    web:
      exposure:
        include: refresh,health,info,bus-refresh

